#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

/**
 * positive_or_negative checks whether i is positive or negative
 */
void positive_or_negative(int i);

/**
 *int largest_number returns the largest number of three
 */
int largest_number(int a, int b, int c);
#endif /* MAIN_H */
