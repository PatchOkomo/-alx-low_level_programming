#include <stdio.h>
/**
 * main - Entry point
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	for (i='A'; i<='Z'; i++)
	{
		int lowercase = tolower(i);
		putchar(lowercase);
	}

	putchar(\n);

	return 0;
}
